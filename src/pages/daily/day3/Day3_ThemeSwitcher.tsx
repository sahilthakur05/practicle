// Day 3 - Q5: Theme Switcher with Context (React)
// Use createContext/useContext — Header toggles, Card reads theme.

import { createContext, useContext, useState } from 'react'

// TODO: Create ThemeContext
// TODO: Create ThemeProvider with state
// TODO: Header component with toggle button
// TODO: Card component that reads theme

export default function Day3_ThemeSwitcher() {
  return (
    <div style={{ padding: 20 }}>
      <h3>Theme Switcher with Context</h3>
      <p>TODO: Wrap Header and Card in a ThemeProvider. Toggle should work from Header, display in Card.</p>
      {/* <ThemeProvider>
        <Header />
        <Card />
      </ThemeProvider> */}
    </div>
  )
}
